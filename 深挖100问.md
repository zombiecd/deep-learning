# 深挖 100 问 · 大规模演练选题

> 这是一份由 AI 老师主动出手的「立题清单」，不是成文文档。10 个主题、每个 10 问、共 100 问。
> 每一问都按本项目的「好问题判据」打磨：有可收敛内核、问"为什么必须这样"、能逼出隐含前提、藏一个反直觉 puzzle、可分层下钻、可验证。
>
> **选题逻辑**：本仓的脊梁是「AI 时代工程师的护城河 = 用深到能看穿对错的心智模型，去当那个接住 AI 错误的保证者」。所以这 10 个主题不是随机铺面，而是**后端系统判断力的承重墙** + **AI 系统工程的交点**——全都选在「AI 经常写出『能跑、看起来对』、却藏着只有心智模型才抓得住的深层错」的地方。学透它们，就是在给自己装那份"被随机翻译器丢掉的保证"。
>
> 主题之间互相穿线（见各问末尾的「呼应」），合起来是一张网，不是十段平行讲义。已成文的三个主题（大模型 / 并发原理 / AI 时代工程师）是这张网的锚点。

---

## ✅ 100 问已全部深讲成文

下面每个主题的 10 问，都已按费曼标准逐一讲透（钩子 → 第一性原理 + 比喻 → 拆穿前提 → 诚实边界 → 可一手验证），见各主题的「深讲十问」：

| # | 主题 | 深讲 | 一句话脊梁 |
|---|---|---|---|
| 一 | 分布式系统 | [深讲十问](./topics/分布式系统/深讲十问.md) | 你永远不知道对方此刻状态，只知道它过去说过什么 |
| 二 | 存储引擎 | [深讲十问](./topics/存储引擎/深讲十问.md) | "写进数据库了"要拆好几层，持久化是和硬件谎言作斗争 |
| 三 | 网络 | [深讲十问](./topics/网络/深讲十问.md) | "连接"是两端共谋的幻觉，可靠是端上模拟出来的 |
| 四 | 可靠性工程 | [深讲十问](./topics/可靠性工程/深讲十问.md) | 失败是设计出来的；重试/超时/冗余本身是放大器 |
| 五 | 可观测性 | [深讲十问](./topics/可观测性/深讲十问.md) | 不能全量观测，可观测性本质是统计推断 |
| 六 | 缓存 | [深讲十问](./topics/缓存/深讲十问.md) | 持有可能过期的副本换速度，难题都是"何时过期"的变体 |
| 七 | 编译器与运行时 | [深讲十问](./topics/编译器与运行时/深讲十问.md) | 源码只是给优化器的意图说明书，不是执行脚本 |
| 八 | 性能工程 | [深讲十问](./topics/性能工程/深讲十问.md) | 直觉对瓶颈几乎总是错的，方法凌驾于技巧 |
| 九 | 安全思维 | [深讲十问](./topics/安全思维/深讲十问.md) | 漏洞都长在没被认出来的信任边界上 |
| 十 | LLM应用工程 | [深讲十问](./topics/LLM应用工程/深讲十问.md) | 可靠不来自模型变可靠，来自你在外面盖的确定工程 |

下面保留的是各主题 10 问的**选题原文**（脊梁 + 问题），作为索引；展开的深讲在上表对应的文件里。

---

## 主题一 · 分布式系统：没有"现在"的世界

> **脊梁**：分布式的第一性原理是——你永远不知道对方此刻的状态，只知道"过去某个时刻它说过什么"。单机里免费的"现在""同时""它是挂了还是只是慢"，跨网络后全部失效。所有难题都是这个失效的推论。

1. 单机里"现在几点"和"A 发生在 B 之前"是免费且确定的。一跨网络，这两个概念为什么**同时**失效？为什么"把两台机器的时钟用 NTP 同步到毫秒"也救不了你——校准了时钟，为什么还是不能用墙上时钟判定事件先后？（这逼出逻辑时钟；Lamport 的 happens-before 和并发主题里的 happens-before 是不是同一个东西？）

2. "节点挂了"和"节点只是网络卡住/很慢"，在另一端看来是**同一个现象**——超时。为什么没有任何算法能在异步网络里区分这两者（FLP）？既然区分不了，那所有心跳、故障检测机制到底在检测什么，它们诚实的语义是什么？

3. CAP 常被讲成"三选二"。为什么这是个误导口诀——P（分区）根本不是你能"选"的，是网络强加的前提？真正的选择只发生在分区的那几秒：一个号称"我们要 CA"的系统，分区时到底会发生什么？为什么说"我要 CA"这句话本身就暴露了没懂 CAP？

4. "强一致"听起来天然正确、该追求。为什么线性一致（linearizability）是个昂贵到大多数系统付不起、也并不需要的保证？最终一致、因果一致、读己之写……这一串"弱"一致性模型，每一档**具体放弃了什么、换回了什么**，谁来决定你的业务该用哪一档？

5. 2PC（两阶段提交）号称能做分布式事务。它最致命的失效点在哪一刻——为什么 coordinator 在"已收齐投票、还没发 commit"时挂掉，会让所有参与者**无限期阻塞**，而这不是实现 bug，是协议的固有缺陷？这把我们逼向了什么（共识 / Saga / TCC）？

6. Paxos/Raft 这类共识算法，本质在解一个什么问题——为什么"让一群机器对一个值达成一致"难到要诺奖级算法，而不是"投票，少数服从多数"这么朴素？多数派（quorum）为什么必须是 N/2+1 而不是别的数，这个数学约束到底在防什么？

7. "Exactly-once delivery（恰好一次投递）"被很多消息队列当卖点。为什么它在网络层面严格来说**不可能**，所谓 exactly-once 其实是 at-least-once + 幂等的障眼法？这个区分对消费端有什么硬性要求——为什么"幂等"才是真正要做的功课？（呼应主题四、主题六）

8. 幂等听起来简单："重复执行结果一样"。但"扣款 100 元"怎么幂等？幂等键怎么设计、存多久、和业务唯一性什么关系？为什么"重复请求""失败重试""并发的同一请求"这三种情况，要不要用同一套机制去挡，本身是个要想清楚的判断？

9. 分布式锁（Redis SETNX / etcd）你以为锁住了。为什么"持锁进程 GC 停顿 30 秒、锁过期被别人抢走、它醒来还以为自己持有"（Kleppmann 的著名质疑）说明：**基于超时的分布式锁，没有 fencing token 就根本不安全**？这和单机锁的本质差别在哪？

10. "脑裂（split-brain）"为什么是高可用系统最怕的词？两个节点都认为自己是 master、各自接受写入——为什么"加个仲裁""谁先抢到谁当 master"这类朴素方案挡不住它？为什么消灭脑裂最终都回到"多数派"这一个答案上？

---

## 主题二 · 存储引擎的物理学：持久化是在和硬件的谎言作斗争

> **脊梁**："写进数据库了"是一句要拆开好几层的话。应用 buffer → 内核 page cache → 磁盘控制器 cache → 盘片，每一层都可能在你以为"落盘"时只是把数据攥在手里。持久化工程的全部，就是和这些"善意的谎言"以及重排序作斗争——这和并发的内存可见性是同一道数学题。

1. `write()` 返回成功，你的数据落盘了吗？为什么没有——它可能还在内核 page cache 里，断电就没了？那 `fsync` 算数吗？为什么连 `fsync` 在某些磁盘/文件系统/虚拟化层上也撒谎（写屏障被吞掉）？"持久化"这个词到底在哪一层才兑现？

2. WAL（预写日志）为什么是几乎所有数据库的命根子？"先写日志再改数据"凭什么能在崩溃后恢复——既然日志本身也要落盘，为什么写两次（日志+数据）反而比直接改数据更安全、还更快？这里的"更快"反直觉在哪（随机写变顺序写）？

3. B+ 树统治索引几十年。为什么它几乎每个设计参数都在迁就同一个事实：磁盘按"页/块"读写、随机寻道极慢？为什么它是"矮胖"的（扇出几百）而非二叉树？为什么叶子串成链表？把它搬到随机访问免费的内存里，它还是最优解吗？

4. LSM-tree（RocksDB/Cassandra）凭什么敢用"把随机写变成顺序追加 + 后台合并"去挑战 B+ 树？它把写放大 / 读放大 / 空间放大三者重新分配了——为什么说没有更好的引擎，只有"针对你的读写比押对了赌注"的引擎？compaction 这个后台活，为什么既是它的优势来源又是它的尾延迟杀手？

5. "数据库帮我保证了 ACID，我不用操心。" 为什么隔离级别（read committed / repeatable read / serializable）一档档放松，本质上和并发主题里"内存模型放松多少乱序自由"是**同构**的？为什么默认隔离级别几乎从不是 serializable——数据库在拿什么换性能，把什么风险（幻读、写偏序）甩回给了你？（呼应主题七、并发主题）

6. MVCC 为什么能"读不阻塞写、写不阻塞读"？它靠什么魔法——为什么"每行存多版本 + 每事务看一个快照"就绕开了读写锁？代价是什么（版本膨胀、vacuum、长事务诅咒）？为什么一个忘了提交的长事务能拖垮整个 PostgreSQL？

7. 索引让查询变快人人皆知。但为什么"加索引"在写密集场景是负优化——每个索引都是一棵要同步维护的树？为什么"覆盖索引""最左前缀"不是数据库的随意规定，而是 B+ 树物理结构的直接推论？为什么 `WHERE a=1 ORDER BY b` 用不用得上同一个索引，取决于索引列的顺序？

8. 为什么"删除"在很多引擎里根本不是删除，而是写一个"墓碑（tombstone）"？为什么逻辑删除的数据要等后台合并才真正消失——而这让"我删了大量数据，磁盘为什么没释放、查询为什么反而更慢了"成了经典翻车现场？

9. 页缓存是操作系统替数据库做的免费缓存。为什么很多数据库会在"信任 OS 缓存"（PostgreSQL）和"自己再建一层 buffer pool"（InnoDB）之间分裂？双重缓存的浪费 vs 自管的可控，这个权衡谁对？为什么 `O_DIRECT` 会存在？（呼应主题六）

10. "顺序写比随机写快"在机械盘上是物理常识（磁头寻道）。到了 SSD 没磁头了，为什么顺序写**依然**更快——这次物理原因完全变了（擦除块、写放大、FTL、GC）？为什么理解"SSD 其实是个伪装成块设备的日志结构系统"会改变你设计存储格式的判断？

---

## 主题三 · 网络："连接"是一种两端共谋的幻觉

> **脊梁**：网络上不存在"连接"这个实体。所谓 TCP 连接，只是通信两端各自在内存里维护的一组状态（序列号、窗口、定时器），靠不断交换的小纸条维持着"我们俩还连着"的共识。一旦接受"连接是幻觉、可靠是端上模拟出来的"，从 TIME_WAIT 到惊群到保活，所有怪现象都顺了。

1. IP 层只承诺"尽力而为"——可能丢、可能乱序、可能重复、可能延迟任意久。TCP 凭什么在这么不靠谱的地基上"凭空"造出一条可靠、有序的字节流？它用哪几样东西（序列号、确认、重传、排序缓冲）把每一种不可靠分别补上？为什么说可靠性从不在网络里，是端上模拟的？

2. 三次握手为什么是三次不是两次？"两次"到底漏掉了什么，使得一个延迟到达的旧连接请求能让服务器建立一个客户端根本不想要的连接？四次挥手又为什么比握手多一次——为什么 TCP 关闭必须两个方向各自独立关？

3. TIME_WAIT：主动关闭的一方为什么要傻等 2×MSL（几十秒到几分钟）才彻底释放？这个"浪费"在防什么——为什么去掉它会让旧连接的迷途报文污染新连接？为什么高并发短连接会被 TIME_WAIT 撑爆端口，而正确解法不是 `tcp_tw_recycle`（已被内核删除）这种危险开关？

4. 拥塞控制（慢启动、拥塞避免、快重传）解决的到底是谁的问题——它不是为了你这条连接快，而是为了所有人共享的网络不崩？为什么 TCP 把"丢包"解读成"网络拥堵"信号，而这个假设在无线/高丢包链路上恰好是错的，导致 wifi 下反而上不去速？BBR 凭什么换个信号（带宽+RTT）就更好？

5. 队头阻塞（head-of-line blocking）在 TCP、HTTP/1.1、HTTP/2 里指的是三个层次上的同一个病。为什么 HTTP/2 在应用层解决了多路复用，却因为底下还是一条 TCP，把队头阻塞"下沉"到了传输层？QUIC 凭什么要搬到 UDP 上重做一个"TCP"才能根治它？

6. Nagle 算法和延迟确认（delayed ACK）单看都是优化，为什么这俩撞一起会产生几十到几百毫秒的诡异延迟（经典 40ms 问题）？为什么这是"两个各自正确的局部优化，组合出全局错误"的教科书案例——和并发里"两个对的局部重排组合出错"是同一类陷阱？（呼应并发主题）

7. "我开长连接（keep-alive）省掉握手，性能就好了。" 为什么长连接引入了握手不必面对的难题：怎么知道对端还活着？为什么 TCP 自带 keepalive 默认要两小时才探测、几乎没用，逼得每个应用层协议（Redis、gRPC、MySQL）自己造心跳？连接池里的"陈旧连接"为什么是经典故障源？

8. MTU 与分片：为什么一个比链路 MTU 大的包被分片后，分片是 TCP/IP 性能与可靠性的隐形杀手？为什么"Path MTU Discovery"依赖的 ICMP 被防火墙粗暴拦掉后，会造成"小请求正常、大请求莫名卡死"这种最难排查的故障（PMTU 黑洞）？

9. TLS 握手到底在防三件不同的事（窃听、篡改、冒充），分别用什么手段？为什么"加密"只解决窃听，而"我连的真的是这个域名而不是中间人"靠的是证书链这套完全不同的信任机制？前向保密（PFS）要求"就算私钥将来泄露、过去抓的包也解不开"，它靠什么数学性质做到？（呼应主题九）

10. DNS 看着只是"域名换 IP"的查号台。为什么它其实是全球最大的最终一致分布式缓存系统——TTL、递归/迭代、负缓存全是分布式缓存的经典权衡？为什么"改了 DNS 半天不生效"和"DNS 劫持"是同一套缓存信任模型的两面？（呼应主题一、主题六，及你的 CDN 背景）

---

## 主题四 · 可靠性工程：失败是设计出来的，不是消除的

> **脊梁**：可靠系统不是"不会失败的系统"，而是"想清楚了每种失败发生后怎么办的系统"。最反直觉的是——那些用来提升可靠性的手段（重试、超时、冗余）本身就是最危险的放大器，能把一次小抖动放大成一场雪崩。这条脊梁是大模型主题"可靠性不来自模型、来自外面的约束"在系统层的投影。

1. "失败了就重试"几乎是本能。为什么重试是双刃剑里更锋利的那一刃——系统已经过载时，重试是火上浇油，能把局部抖动放大成全局雪崩（retry storm）？为什么"指数退避 + 抖动（jitter）"里的 jitter 不是锦上添花，而是防止所有客户端同时重试这个致命同步的关键？

2. 超时设多少？这个看似拍脑袋的数字，为什么是分布式系统里最重要也最常设错的参数？为什么"设太长"会让故障扩散、线程池被占满，"设太短"又会误杀本会成功的请求并触发重试风暴？为什么超时必须是"预算"而非"常量"——上游的超时怎么传给下游？

3. 熔断器（circuit breaker）为什么本质是一个"用快速失败保护下游"的状态机？关闭/打开/半开三个状态各自在赌什么？为什么"半开"是整个熔断器最微妙的地方——它怎么在"试探恢复"和"别再压垮刚缓过来的下游"之间走钢丝？

4. 背压（backpressure）为什么是比"加大缓冲/加大队列"高一个维度的解法？为什么"队列满了就扩容队列"几乎总是错的——无界队列怎么把"快速失败"偷偷变成"无限延迟 + OOM"？为什么健康的系统必须有能力对上游说"我现在不行，你慢点"？（呼应并发主题 channel 的有界缓冲）

5. 级联失败（cascading failure）：为什么一个非关键下游变慢，能拖垮整条调用链上每个上游？线程/连接被慢请求占住不放——为什么这是"资源耗尽型"故障的核心机制？舱壁隔离（bulkhead）凭什么靠"分池"就能阻断这种传染？

6. SLO 与错误预算（error budget）为什么是反直觉的"主动允许失败"？为什么追求 100% 可用是工程上的错误目标（成本无穷且没必要）？错误预算怎么把"该不该发新版本"从拍脑袋争论变成一道算术题——预算还有就发、烧光就冻结？（呼应主题八、主题十）

7. 健康检查你以为很简单："活着返回 200"。为什么"浅检查"（进程活着）和"深检查"（依赖都正常）是两个会互相打架的东西？为什么深健康检查 + 自动摘除节点，能在依赖抖动时把"个别节点慢"放大成"所有节点被摘、服务全挂"？

8. 优雅降级的本质是什么——为什么真正可靠的系统是"零件坏掉时还能提供核心功能的残缺服务"，而非"全有或全无"？怎么在设计期就区分"核心路径"和"可丢弃的增强"？为什么降级开关必须事故前就埋好且演练过，临时加根本来不及？

9. 为什么"重启大法"能解决大量线上问题，但**依赖**重启恰恰是不可靠的症状而非解药？什么故障重启能救（状态泄漏、内存碎片、卡死），什么重启反而更糟（脏数据、重启风暴、丢在途请求）？为什么"能被随时安全杀掉"（crash-only / 幂等启动）本身是一种该追求的设计美德？

10. 混沌工程为什么是"主动制造故障"这种反直觉做法——它在赌一个什么前提？为什么"没演练过的故障预案等于没有"？为什么一个从没被真正断过网、杀过主库的"高可用"架构，它的高可用只在 PPT 上，你对它的信心是没有证据的？（呼应元认知"感觉良好≠做对了"）

---

## 主题五 · 可观测性的认识论：在不能全量观测的约束下重建真相

> **脊梁**：你永远无法完整观测一个生产系统——全量记录的成本会超过系统本身。所以可观测性本质是一个统计推断问题：用有限的、有偏的、被采样的信号去重建系统的真实状态。意识到"你盯着的监控大盘是一个被各种偏差扭曲的影子"，你才算真正会看监控。

1. Metrics、Logs、Traces 被称为三支柱。为什么它们不是"同一信息的三种格式"，而是在"基数 ↔ 成本 ↔ 可回溯性"上做了三种不同取舍的东西？为什么 metrics 便宜但丢了个体、logs 全但贵、traces 能串因果但要采样——为什么没有一种能同时做到全、便宜、可下钻？

2. 为什么平均延迟几乎是最没用、最骗人的指标？为什么一个 avg 50ms 的服务可能有 1% 的用户在忍受 5 秒？为什么必须看 p99/p999——而 p99 自己也藏着更深的谎言：你在客户端测的 p99 和服务端测的 p99 为什么能差一个数量级？

3. 协调遗漏（coordinated omission）为什么是性能测量里最普遍、最隐蔽的造假源？为什么"系统卡住的那段时间，恰恰是你的压测工具因为发不出请求而没记录的那段时间"，导致最糟的延迟被系统性漏掉？为什么这意味着市面上大量 benchmark 数字乐观到失真？（可用 wrk vs wrk2 一手复现）

4. 采样：既然全量 trace 太贵就采 1%。为什么"头部采样"（请求一进来就掷骰子）会系统性漏掉那些罕见的、恰恰是你最想看的出错请求？尾部采样凭什么更好却更难实现（要先缓存完整 trace 再决定）？采样率这个数字背后藏着什么认识论代价？

5. 高基数（high cardinality）为什么是监控系统的成本核弹？为什么"按 user_id 打标签"能让 Prometheus 内存爆炸，而这恰恰是排障时最想要的维度？为什么这个矛盾（最想下钻的维度=最贵的维度）逼出了一整个行业（列存、ClickHouse 这类）来重做可观测性后端？（呼应你的 ClickHouse 经验）

6. 为什么"日志记全一点总没错"是个昂贵错觉？日志的成本不在写，在"传输+索引+存储+查询"——为什么 INFO 级日志在高 QPS 下能比业务流量还大？结构化日志凭什么不只是"好看"，而是把日志从"给人读的字符串"变成"可查询的数据"的关键一跃？

7. 链路追踪怎么在几十个服务、上千次调用里把"同一个用户请求"串成一条线？trace id / span id 怎么跨进程、跨线程、跨异步回调传递（context propagation）——为什么这个透传在 Go 里是 `context.Context`、在异步/消息队列里就成了最容易断链的地方？

8. "告警太多，结果谁都不看了"——告警疲劳为什么比"没监控"更危险？为什么好告警应基于"症状"（用户受影响了吗）而非"原因"（CPU 高了）？为什么"CPU 80% 就告警"这类阈值告警是告警疲劳的头号制造者？（呼应主题四 SLO）

9. 为什么"能观测"和"能理解"之间还隔着一道鸿沟——你有了全套 dashboard，为什么半夜出事还是抓瞎？什么叫"可观测性的最后一公里是人的心智模型"：为什么没有系统架构的 mental model，再多指标也只是噪音？（直接呼应"你无法验证你无法建模的东西"）

10. 观察者效应在软件里真实存在吗？为什么加上详细的 trace/log/profiling 本身会改变被观测系统的行为（拖慢、改变时序、甚至让偶发并发 bug 消失——海森堡 bug）？为什么"开了 debug 日志 bug 就不复现了"不是玄学，而是观测扰动了时序？这对排查并发问题意味着什么策略？（呼应并发主题）

---

## 主题六 · 缓存：用"可能过期的副本"换速度

> **脊梁**：缓存的本质是"持有一份可能已经过时的副本来换速度"。所以缓存的一切难题都是同一个问题的变体：这份副本什么时候、凭什么、由谁判定它过期了？"缓存失效是计算机科学两大难题之一"不是段子，因为它逼你在"新鲜度、可用性、一致性"这个不可能三角里当场选边。

1. 为什么 Phil Karlton 那句"计算机科学只有两件难事：缓存失效和命名"是真理而非玩笑？为什么"失效"难到这个地步——它要回答的是一个分布式 + 时间 + 一致性纠缠在一起的问题：这份副本和源数据现在还一致吗，而你恰恰无法廉价地知道答案？（呼应主题一）

2. 缓存穿透、击穿、雪崩——为什么这三个听起来像同义词的故障，其实是三个完全不同的失效机制（穿透=查不存在的 key 绕过缓存打到库；击穿=热点 key 过期瞬间大量请求涌入；雪崩=大量 key 同时过期/缓存整个挂了）？为什么它们的解法（布隆过滤/空值缓存、互斥重建/singleflight、过期打散）各管各的，用错了不解决问题？（直接呼应你的 singleflight demo）

3. Cache-aside、Read-through、Write-through、Write-back 这几种模式，为什么不是"四选一的风格偏好"，而是在"一致性、延迟、丢数据风险"上各踩不同点？为什么最常用的 Cache-aside 恰恰有个经典并发竞态（读到旧值后写回、覆盖了别人的新值），而它至今没有完美解、只有缓解（延迟双删、订阅 binlog）？

4. "先更新数据库再删缓存"还是"先删缓存再更新数据库"——这个看似抠字眼的顺序问题，为什么是工程上真会翻车的并发竞态？为什么"更新缓存"（而非"删除缓存"）几乎总是更糟的选择？为什么终极答案常常是"别追求强一致，配个兜底 TTL + 接受短暂不一致"？

5. TTL 这个最朴素的失效手段，为什么其实是"用一段可容忍的陈旧时间，换掉了主动失效的全部复杂度"？为什么"TTL 设多长"是业务判断而非技术判断——它直接等价于"你的业务能忍受数据陈旧多久"？为什么给所有 key 设同一个 TTL 是在亲手制造雪崩？

6. 多级缓存（浏览器→CDN→网关→本地→Redis→DB）为什么每多一级就多一处"可能各自持有不同版本"的不一致源？为什么 CDN 缓存的失效（purge）出了名的慢和不可靠，逼出"版本化 URL / 内容指纹"这种"从不失效、只换名字"的釜底抽薪打法？（呼应主题三 DNS 缓存、你的 CDN 背景）

7. 本地缓存（进程内）看似最快，为什么却引入了 Redis 这类集中式缓存没有的难题：N 个实例各有一份副本，怎么同时失效？为什么这又把你拽回分布式一致性（广播失效、版本号、最终一致）？什么时候本地缓存的"快"根本补偿不了它的"各执一词"？（呼应主题一）

8. 缓存命中率是不是越高越好？为什么盲目追求高命中率会让你缓存大量"占地方但很少被再读"的冷数据？为什么 LRU 在扫描型负载（一次性遍历大量数据）下会被"污染"到形同虚设，而 LRU-K / LFU / ARC 这些算法到底在和什么样的访问模式作斗争？（可用模拟访问序列复现）

9. 为什么"加缓存"经常是把一个性能问题换成两个一致性问题，而很多时候正确判断是"先别加缓存"？怎么判断一个慢查询该被缓存、还是该去优化索引/查询本身（呼应主题二）？为什么"缓存是用来扛读放大的，不是用来补烂查询的"能省掉一类自找的麻烦？

10. Redis 你以为是"内存里的哈希表"。为什么它的持久化（RDB 快照 vs AOF 日志）面对的是和数据库 WAL 一模一样的"崩溃一致性"难题（呼应主题二）？为什么单线程的 Redis 反而快，而一个 `KEYS *` 或一个大 key 的删除能把这个单线程卡死、拖垮所有其他请求？这对你用 Redis 的判断有什么硬约束？

---

## 主题七 · 编译器与运行时：你写的代码不是真正运行的代码

> **脊梁**：你写的源码只是给优化器的一份"意图说明书"，不是执行脚本。编译器和运行时会内联、逃逸、消除、重排、回收——真正跑起来时，和你写的那行常常对不上。机械同理心的核心，就是看穿这层"源码→实际执行"的映射，否则你对性能的一切判断都是猜的。

1. 你 `new` 了一个对象，它一定在堆上吗？为什么 Go 的逃逸分析能让一个"看起来在堆上"的对象实际待在栈上（或反过来）？为什么"返回局部变量的指针"在 C 里是悬垂指针灾难、在 Go 里却安全——编译器替你做了什么决定？为什么搞懂逃逸分析能直接改变你写 GC 友好代码的判断？（可用 `go build -gcflags=-m` 一手验证，呼应你 lab 的反汇编经验）

2. 内联（inlining）为什么是"优化之母"——不是因为省了那一次函数调用开销，而是因为内联打开了后续一连串优化（常量传播、死代码消除、逃逸分析）的大门？为什么"函数太大就不内联"这个阈值能让你的热点函数性能悬崖式下跌？为什么有时手动把大函数拆小反而更快？

3. GC 到底什么时候、为什么会"偷走"你的 CPU 和延迟？为什么三色标记 + 写屏障能做到"大部分并发、只短暂 STW"，而这个"写屏障"和并发主题里的内存屏障是不是亲戚？为什么"减少对象分配"比"调 GC 参数"更治本——为什么 GC 调优的第一性原理是"别产生垃圾"而非"更快地收垃圾"？（呼应并发主题）

4. 为什么一个 `interface{}` 赋值会"偷偷"产生一次堆分配（装箱）？为什么接口方法调用比直接调用慢（动态派发、无法内联），而编译器的去虚化（devirtualization）有时能救回来？为什么"面向接口编程"的优雅在热点路径上是有价格的——这个价格什么时候该付、什么时候不该？（呼应你延伸阅读里 "What Color is Your Function" 的抽象代价主题）

5. 数组越界检查：Go/Rust 每次 `a[i]` 都在背后插了一道检查，为什么这不会让它们比 C 慢一大截——边界检查消除（BCE）在什么条件下能证明"这个索引不可能越界"从而删掉检查？为什么写循环的方式（`for i := range a` vs `for i := 0; i < len(a)`）会影响 BCE 能不能生效？（可用反汇编验证，呼应 lab）

6. CPU 分支预测：为什么"对一个已排序数组做条件求和"能比对未排序的快好几倍，明明指令完全一样（那道著名的 StackOverflow 神题）？为什么分支预测失败的代价是几十个周期的流水线清空？这对你写热点判断逻辑（把大概率分支放前面、用无分支技巧）意味着什么？（可一手 benchmark 复现）

7. 数据的内存布局（struct 字段顺序、对齐、padding）为什么能让两个字段完全相同、只是顺序不同的 struct 占不同内存、缓存表现差一截？为什么"热字段放一起、冷字段放一起"（hot/cold splitting）是高性能数据结构的基本功？这和并发主题里的伪共享（false sharing）是不是同一层的考量？（呼应并发主题 C 墙）

8. 为什么"零成本抽象"（zero-cost abstraction）这个 C++/Rust 的招牌口号，既是真的、也是个要打星号的承诺？什么抽象真能被编译器抹平到零（泛型单态化、迭代器），什么抽象注定有运行时成本（虚函数、动态类型、GC）？为什么 Go 的泛型选了"字典 + 部分单态化"而非 C++ 的全单态化，这个取舍换来什么、失去什么？

9. 为什么同一份 Go 代码，编译产物里藏着大量你没写的东西——栈扩容检查、抢占点、GC 写屏障、race detector 插桩？为什么"goroutine 栈能动态增长"这件事要求编译器在几乎每个函数入口插一段检查（morestack）？理解"运行时往你代码里塞了什么"为什么是判断 Go 性能行为的前提？

10. JIT（如 JVM）和 AOT（如 Go/Rust）的根本分歧是什么——为什么"运行时才编译"反而可能比"提前编译"跑得更快（基于真实运行 profile 做激进优化、去虚化、热路径内联）？为什么 JIT 的代价是启动慢、预热期、不可预测的延迟尖峰？这个权衡怎么决定了"Java 适合长跑服务、Go 适合 CLI 和冷启动敏感场景"？

---

## 主题八 · 性能工程：你以为的瓶颈几乎总是错的

> **脊梁**：性能工程的第一性原理是——人的直觉对"瓶颈在哪"几乎总是错的，所以方法（测量、建模、定位）凌驾于技巧之上。优化没测量过的地方，等于在黑暗里装修。这条脊梁是元认知"感觉良好≠做对了"的硬核版。

1. 为什么"凭直觉优化"是性能工程的头号原罪——程序员对热点的猜测准确率低到可笑，真正瓶颈往往在你完全没想到的地方（一次意外的 N+1、一个隐藏的锁、一次序列化）？为什么"先 profile 再优化"不是谨慎，是唯一理性的起点？（呼应元认知 spec>印象）

2. Amdahl 定律为什么给所有并行优化判了死刑上限——一个任务若 5% 是串行的，哪怕无限核，最多也只能加速 20 倍？为什么这意味着"加机器/加线程"的回报会断崖式递减？通用可扩展性定律（USL）又多算了一项什么成本（协调开销），解释了为什么加机器到某个点之后系统反而更慢？（呼应并发主题"核越多越慢"）

3. 延迟和吞吐为什么经常是此消彼长的冤家，优化一个会害了另一个？为什么批处理（batching）能提吞吐却必然增加延迟？为什么"我能扛 10 万 QPS"和"我单请求 10ms 返回"必须分开问、且要问"在什么负载下"？利特尔法则（Little's Law）怎么把这三者（并发数、吞吐、延迟）用一个公式锁死？

4. 为什么一个系统在 50% 负载下延迟很好、80% 还行、过了某个点延迟突然垂直拉升（排队论的"曲棍球棒"）？为什么这条曲线不是线性的、而 M/M/1 模型告诉你"利用率越接近 100%，排队延迟趋向无穷"？为什么这意味着"把服务器跑到 90% 利用率"是在悬崖边跳舞？

5. 为什么微基准测试出了名地会骗人——编译器把测试代码优化掉了（死代码消除）、CPU 缓存/分支预测被预热到不真实、JIT 还没编译？为什么 `go test -bench` 的结果要警惕这些陷阱？为什么"基准跑得快"和"生产跑得快"之间隔着一整个真实负载的鸿沟？（呼应主题五 coordinated omission）

6. 为什么尾延迟在大规模系统里被放大成主宰因素——一个请求要扇出到 100 个服务，只要每个有 1% 概率慢，整个请求慢的概率就接近 63%？为什么"p99 在单机不重要、在分布式致命"？对冲请求（hedged requests）凭什么用"多发一份、谁快用谁"砍尾延迟，代价是什么？

7. 内存带宽和缓存，为什么在现代 CPU 上经常比"算得快不快"更决定性能——为什么"CPU 大部分时间在等内存"（memory wall）？为什么遍历链表比遍历数组慢得多，明明算法复杂度都是 O(n)（缓存局部性、预取失效）？为什么"复杂度相同"根本不代表"实际速度相同"？（呼应主题七内存布局、并发缓存墙）

8. 为什么"加缓存""加索引""加机器"这三板斧经常是在治标——它们什么时候是对的，什么时候只是把架构问题往后拖？怎么区分"这是个需要更多资源的容量问题"和"这是个再多资源也填不满的设计问题（O(n²) 算法、放大效应）"？为什么搞错这个区分会让你烧很多钱还是慢？

9. 性能里的"二八"为什么常常是"九九一"——99% 的时间花在 1% 的代码上？为什么这意味着大规模"优化"代码是浪费，而精准定位那 1% 才是全部价值？火焰图凭什么是定位那 1% 最高效的可视化——怎么一眼看出"宽的是热点、不是高的"？

10. 为什么"过早优化是万恶之源"这句被引用到滥的话，其后半句"但别放过那关键的 3%"才是 Knuth 的真意，而大多数人只记住前半句给自己偷懒找借口？怎么校准"什么时候该停止优化"——为什么"够快了"是个必须用 SLO/业务需求定义、而非用"还能更快"定义的判断？（呼应主题四 SLO、元认知收口回路）

---

## 主题九 · 安全思维：信任边界与"一切输入皆敌"

> **脊梁**：安全不是一个功能模块，是一种看系统的视角——在哪里数据从"我控制的"变成"别人能影响的"，那条线就是信任边界，而所有漏洞都长在没认出来的信任边界上。养成"这个数据是谁能控制的"这个条件反射，注入、越权、SSRF 这些看似无关的漏洞会显出同一个骨架。这正是 AI 写代码时最系统性的盲区，也因此是"保证者"最该握紧的判断。

1. 为什么几乎所有注入类漏洞（SQL 注入、命令注入、XSS、模板注入）都是同一个根本错误的不同外衣——"把不可信的数据当成可信的代码/结构来解释"，即数据与控制流的混淆？为什么"参数化查询"能根治 SQL 注入而"转义/过滤"只是打补丁——它们的差别是种类上的还是程度上的？

2. 为什么"前端做了校验"在安全上等于没做——为什么信任边界永远在服务端，客户端的一切（包括它发来的"我是管理员"标记）都必须当作敌人伪造的？为什么这条原则能一句话判死一大类"看起来安全"的设计（比如靠隐藏按钮做权限控制）？

3. 认证（你是谁）和授权（你能干啥）为什么是两件必须分开、又最常被混在一起做错的事？为什么绝大多数严重越权漏洞（IDOR、水平/垂直越权）不是"认证被绕过"，而是"认证对了、但根本没做授权检查"——系统验明了你是张三，却没问"张三能看李四的订单吗"？

4. 为什么密码绝对不能明文存、甚至不能用普通哈希（MD5/SHA256）存，而必须用慢哈希（bcrypt/argon2）加盐？为什么"慢"在这里反而是安全特性——它在和什么（GPU 暴力破解、彩虹表）赛跑？为什么"加盐"防的是一类完全不同于"加密"的攻击？理解这个能让你一眼识破大量错误的"密码加密"方案。

5. 为什么"自己实现加密算法"是安全领域人人喊打的禁忌——不是因为你写不出 AES，而是因为安全性藏在你想不到的地方（时序侧信道、随机数质量、填充、IV 复用、密钥管理）？为什么一个"功能上完全正确"的加密实现，可以因为"比较 MAC 时用了非常量时间的字符串比较"而被时序攻击攻破？（这是"能跑≠对"的极端案例，呼应元认知）

6. SSRF（服务端请求伪造）为什么是云时代杀伤力被放大的漏洞——为什么"让服务器去访问一个用户提供的 URL"这个看似无害的功能，能被用来读云元数据接口（169.254.169.254）偷走整个云账号的凭证？为什么"校验 URL 不是内网地址"这种防御几乎总被花式绕过（DNS rebinding、重定向、奇异 IP 表示法）？（呼应主题三）

7. 为什么 CSRF 和 XSS 听起来像、本质却相反——XSS 是"网站信任了用户输入的脚本"，CSRF 是"网站信任了浏览器自动带上的凭证"？为什么 SameSite cookie 几乎改变了 CSRF 的攻防格局？为什么"用 GET 做有副作用的操作"是同时违反 REST 语义和打开 CSRF 大门的双重错误？

8. 最小权限原则为什么是纵深防御的地基——为什么"这个服务用 root 跑""这个库账号有所有权限""这个 token 永不过期"是把一次小突破放大成全面沦陷的杠杆？为什么"假设你已经被攻破了，攻击者能横向移动多远"是比"怎么不被攻破"更成熟的安全思维（assume breach）？

9. 为什么"供应链"成了现代安全最软的腹部——你的代码可能固若金汤，但 `npm install` 进来的几百个依赖里任一个被投毒，整条线就破了？为什么 lockfile、依赖审计、最小依赖不是洁癖而是必需？为什么"一个看似人畜无害的小工具库被收购/被劫持"是真实发生过且会再发生的攻击面？（呼应你 git-workflow 里"动 lock 文件要确认"的纪律）

10. 为什么安全和可用性、性能常常直接对立（多一道校验慢一点、多一层加密费一点、最小权限麻烦一点），而"安全是个权衡而非绝对"这个认识反而是专业的标志？怎么用威胁建模（threat modeling）把"我们要安全"这种空话，逼成"我们防谁、防什么、不防什么、防到什么程度"的工程判断？（呼应主题四 SLO 的"主动定义可接受风险"）

---

## 主题十 · LLM 应用的系统工程：在概率地基上盖确定的房子

> **脊梁**：把一个会编造、不确定、有损的模型，工程化成一个敢交付的系统——靠的从来不是模型本身变可靠，而是你在它外面盖的那一圈确定的工程（接地、约束、验证、反馈）。这是大模型主题"靠谱从哪来"的落地版，也是后端工程师的判断力在 AI 时代最值钱的变现处：你比模型更懂系统。

1. 上下文窗口为什么不是"内存条越大越好"那种线性资源——为什么注意力机制的成本随上下文长度是 O(n²) 的，这个平方项怎么决定了"长上下文"的真实价格（钱、延迟、甚至质量下降）？为什么"把所有资料一股脑塞进 prompt"在工程上是又贵又笨、还会触发"大海捞针"失效（lost in the middle）的反模式？

2. RAG（检索增强生成）的本质到底是什么——为什么它不是"让模型更聪明"，而是"在生成前把对的事实接到模型嘴边"，是给一个会编造的系统装外部记忆和事实接地？为什么 RAG 的成败 90% 在"检索"那一半（召回了对的片段吗）而非"生成"那一半，而大多数人却把精力花在调 prompt 上？（呼应大模型"外部接地"）

3. Embedding 和向量检索：为什么"把文本变成几百维向量、用余弦相似度找近邻"能捕捉"语义相似"而不只是"字面相似"？为什么这套方法有个根本局限——它擅长"模糊语义匹配"却搞不定"精确条件过滤"（如"2023 年之后、金额>100 的"），逼出了混合检索（向量+关键词+结构化过滤）？为什么"语义相似"有时恰恰召回了"看起来像但事实相反"的内容？

4. 为什么微调（fine-tune）经常是被误用的"银弹"——为什么"模型不懂我的业务知识，那就拿业务数据微调"这个直觉是错的，微调改的是"风格/格式/行为"而非"灌注新事实"，灌事实是 RAG 的活？怎么判断一个需求该用 prompt 工程、该用 RAG、还是该用微调——这三者的决策边界在哪？

5. 为什么 LLM 应用的"评估（eval）"是整个工程里最被低估、又最决定生死的一环——传统软件你有确定的单测（输入 X 必出 Y），一个输出不确定的系统怎么测？为什么"跑几个例子看着对就上线"是 AI 应用最大的工程不成熟，而建一套 eval 集 + 用 LLM 当裁判（LLM-as-judge）本身又引入了什么新的不可靠？（呼应测试纪律 + 元认知"感觉良好≠做对了"）

6. Function calling / tool use 凭什么让一个只会输出文字的模型"动手做事"——为什么它的本质是"模型输出一个结构化的意图，由你的确定性代码去执行"，真正干活的安全保证在你的代码这一侧而非模型侧？为什么"让模型直接生成 SQL/shell 去执行"是把信任边界放错了地方的危险设计？（直接呼应主题九信任边界 + 大模型"收尾权"）

7. Agent 的多步自主为什么是可靠性的噩梦放大器——为什么单步 95% 正确的模型，跑 10 步自主任务整体成功率会暴跌（误差复利）？为什么"让 agent 自由发挥 N 步"和"把任务拆成有验证点的短链"是成熟度的分水岭？（直接呼应大模型 Q2 你已成文的"误差复利"，在这里落到 agent 工程）

8. 幻觉在工程上为什么无法"消除"只能"围栏"——为什么它是概率生成的内禀属性而非 bug？围住它的几种确定性手段（强制引用来源、结构化输出 + schema 校验、检索接地、置信度门控、人在环）各自补的是哪一类幻觉？为什么"加一句 prompt 让它别编造"几乎没用？（呼应大模型"两层幻觉"）

9. 为什么 LLM 应用的成本和延迟必须在设计期就当一等公民来建模，而不是事后优化——token 计费、流式输出、KV cache、prompt caching、模型分级路由（小模型挡大部分、大模型兜底）这些手段，分别在和成本/延迟的哪一项作斗争？为什么"无脑全用最强模型"在生产里是会破产的判断？（呼应主题八 + 你 performance.md 的模型分级思想）

10. 提示词注入（prompt injection）为什么是 LLM 时代独有、且至今没有根治方案的安全难题——为什么它和 SQL 注入"同形"（数据与指令混淆）却比 SQL 注入更难防（因为模型的"指令"和"数据"本就是同一片自然语言，无法参数化）？为什么"间接注入"（模型读取的网页/文档里藏着指令）让接了工具的 agent 成为重灾区？这对你设计 AI 系统的信任边界意味着什么硬约束？（呼应主题九注入与信任边界，收口整张网）

---

## 怎么用这份清单

- 每个主题都已经按本仓格式备好了「脊梁」一句话，可以直接当未来 `topics/X/README.md` 的开篇。
- 不必按顺序。挑一个此刻最有"张力"的问题开挖，按工作循环（我详讲 → 你质疑 → 抠细节 → 成文）走。
- 这 100 问只是**入口**；真正的下钻、勘误、最小复现、一手出处，是在每次具体协作里长出来的。清单负责"问得好"，不负责"答得对"——答得对要回一手材料。
- 主题间的「呼应」是有意埋的线：学到第三个主题时，前两个会回来找你。这正是"把知识学到见底"该有的样子——底层是连通的。
